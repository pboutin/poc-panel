<label class="panel {{if isSelected 'panel-success' 'panel-info'}}">
    <div class="panel-heading">
        <input type="radio" name="{{group}}" checked="{{if isSelected 'checked'}}" />
        {{title}}
    </div>
    <div class="panel-body">
        {{yield}}
    </div>
</label>
